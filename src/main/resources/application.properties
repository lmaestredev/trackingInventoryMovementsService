server.port=${trackingInventoryPort}
spring.data.mongodb.uri=${mongoUri}
spring.data.mongodb.database=${trackingInventoryDatabase}
rabbitUri=${rabbitUri}
rabbitExchange=${rabbitExchange}
customerRoutingKey=${customerRoutingKey}
productRoutingKey=${productRoutingKey}
saleRoutingKey=${saleRoutingKey}
errorRoutingKey=${errorRoutingKey}
customerQueue=${customerQueue}
productQueue=${productQueue}
saleQueue=${saleQueue}
errorQueue=${errorQueue}
server.netty.lifecycle.read-timeout=10000
